package edu.nyu.oop.constructs;

import java.util.List;

public interface JScope {
    // no longer used, needed it when we explicitly used namespace identifiers for everything
    public String getName();
    public List<String> getPkg();
}
